import 'package:flutter/widgets.dart';

class FFEventCard extends StatelessWidget {
  const FFEventCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
